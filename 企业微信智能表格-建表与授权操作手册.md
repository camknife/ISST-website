# 企业微信智能表格 · 建表与授权操作手册

> 适用场景：在自建应用「课程网站--数据接入」下创建新的智能表格，并授权指定成员（含其他部门）管理。
> 所有命令在云服务器终端（SSH）执行。

---

接口api文档来源：

[新建文档 - 文档 - 企业微信开发者中心](https://developer.work.weixin.qq.com/document/path/97460)

[查询记录 - 文档 - 企业微信开发者中心](https://developer.work.weixin.qq.com/document/path/101158)

[企业微信智能表格id，及其子表id和视图id如何查看获取？ - 开发者社区 - 企业微信开发者中心](https://developer.work.weixin.qq.com/community/question/detail?content_id=16770987847733036921)



## 一、凭证与现有资源

### 1.1 核心凭证（绝密，只放服务器，不给任何人）

| 凭证 | 值 | 在哪查 |
|------|-----|--------|
| 企业 ID（CorpID） | `（你自己保存的值）` | 管理后台 → 我的企业 → 企业信息 |
| 应用 Secret | `（你自己保存的值）` | 管理后台 → 应用管理 → 课程网站--数据接入 |
| 应用 AgentID | `（你自己保存的值）` | 同上 |

### 1.2 已创建的表格

| 表名 | docid | sheet_id | 用途 |
|------|-------|----------|------|
| PAK课程信息表-API | `dckdMmt5rBcgwCHtOHa9vv2GIjcH63IjkN8Q3btKFXHnUzHzQ3_z60U88Acxaj3QRZRI6VoaDlx7OQdIjzpNqkag` | `q979lj` | 网站A 课程信息读写 |
| PAK用户咨询信息表 | `dcucwRlsuqdGk3_nUonrig6DWf3udT_9yGRC9pf1QdwJF3WOIDbTHK96P1rGfUTNkY9oPDRGgHRxwxrQ6yVA9dqQ` | `q979lj`（以 get_sheet 为准） | 网站A 接收前端表单 |

> 表格链接在企微客户端「文档」里可找到，或用建表时返回的 url。

### 1.3 关键人员 userid

| 姓名 | userid | 备注 |
|------|--------|------|
| 许翼飞 | `XuYiFei` | 本人 |

> 其他成员的 userid 查询方法见第五节。

---

## 二、标准流程：4 步建新表并授权

### 第 0 步：获取 access_token（有效期 2 小时）

浏览器打开（替换 CorpID 和 Secret）：

```
https://qyapi.weixin.qq.com/cgi-bin/gettoken?corpid=你的企业ID&corpsecret=你的Secret
```

复制返回的 `access_token`，在终端赋值：

```bash
TOKEN="粘贴access_token"
```

> token 过期会报 40014/42001，重新获取即可。

### 第 1 步：创建智能表格

```bash
curl -X POST "https://qyapi.weixin.qq.com/cgi-bin/wedoc/create_doc?access_token=$TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"doc_type":10,"doc_name":"新表名称"}'
```

返回示例：

```json
{"url":"https://doc.weixin.qq.com/smartsheet/s3_xxx","docid":"dcXXXX...","errcode":0,"errmsg":"ok"}
```

⚠️ **docid 只返回这一次，立刻保存到本手册 1.2 表格里！**

### 第 2 步：查询子表 sheet_id

```bash
curl -X POST "https://qyapi.weixin.qq.com/cgi-bin/wedoc/smartsheet/get_sheet?access_token=$TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"docid":"第1步保存的docid"}'
```

返回 `sheet_list` 里的 `sheet_id`（新建表默认一般是 `q979lj`）。

### 第 3 步：添加管理员成员

```bash
curl -X POST "https://qyapi.weixin.qq.com/cgi-bin/wedoc/mod_doc_member?access_token=$TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"docid":"新表docid","update_file_member_list":[{"type":1,"auth":7,"userid":"成员userid"}]}'
```

一次加多人（例如自己 + 其他部门负责人）：

```bash
curl -X POST "https://qyapi.weixin.qq.com/cgi-bin/wedoc/mod_doc_member?access_token=$TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"docid":"新表docid","update_file_member_list":[{"type":1,"auth":7,"userid":"XuYiFei"},{"type":1,"auth":7,"userid":"对方userid"}]}'
```

权限值说明：

| auth | 权限 | 适用对象 |
|------|------|---------|
| `1` | 只读 | 只需查看的人 |
| `2` | 可编辑 | 日常改数据的维护者 |
| `7` | 管理员 | 负责人（可再拉其他人协作） |

### 第 4 步：建字段（客户端操作，最快）

1. 用企微客户端打开新表链接
2. 点击表头「+」逐个添加字段，设置字段名和类型
3. 字段名必须与后端代码里的 key **完全一致**（含空格）

---

## 三、一键脚本（可选）

保存为 `create_table.sh`，以后建表一条命令搞定：

```bash
#!/bin/bash
# 用法: ./create_table.sh 表名称 管理员userid1 管理员userid2 ...
# 示例: ./create_table.sh "PAK网站B课程表" XuYiFei ZhangSan

CORP_ID="你的企业ID"
SECRET="你的Secret"
TABLE_NAME="$1"

if [ -z "$TABLE_NAME" ]; then echo "用法: $0 表名称 userid1 [userid2 ...]"; exit 1; fi

# 1. 获取 token
TOKEN=$(curl -s "https://qyapi.weixin.qq.com/cgi-bin/gettoken?corpid=$CORP_ID&corpsecret=$SECRET" | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 2. 建表
RESULT=$(curl -s -X POST "https://qyapi.weixin.qq.com/cgi-bin/wedoc/create_doc?access_token=$TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"doc_type\":10,\"doc_name\":\"$TABLE_NAME\"}")
echo "建表结果: $RESULT"

DOC_ID=$(echo "$RESULT" | python3 -c "import sys,json;print(json.load(sys.stdin)['docid'])")

# 3. 组装成员列表（全部给管理员权限）
MEMBERS=$(python3 -c "
import json,sys
users = sys.argv[1:]
print(json.dumps([{'type':1,'auth':7,'userid':u} for u in users]))
" "$@")

# 4. 授权
curl -s -X POST "https://qyapi.weixin.qq.com/cgi-bin/wedoc/mod_doc_member?access_token=$TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"docid\":\"$DOC_ID\",\"update_file_member_list\":$MEMBERS}"
echo ""
echo "✅ 完成。请把上面的 docid 记录到操作手册！"
```

---

## 四、读写命令模板（测试用）

### 读取记录

```bash
curl -X POST "https://qyapi.weixin.qq.com/cgi-bin/wedoc/smartsheet/get_records?access_token=$TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"docid":"表docid","sheet_id":"子表id"}'
```

### 写入记录（用中文字段名做 key）

```bash
curl -X POST "https://qyapi.weixin.qq.com/cgi-bin/wedoc/smartsheet/add_records?access_token=$TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "docid":"表docid",
    "sheet_id":"子表id",
    "key_type":"CELL_VALUE_KEY_TYPE_FIELD_TITLE",
    "records":[{"values":{
      "字段名1":[{"type":"text","text":"值1"}],
      "字段名2":[{"type":"text","text":"值2"}]
    }}]
  }'
```

> 文本字段值必须包成 `[{"type":"text","text":"值"}]`；数字、日期字段直接传值（日期传毫秒时间戳字符串）。

---

## 五、查询其他部门成员的 userid

1. 登录企业微信管理后台 → 左侧「通讯录」→「成员管理」
2. 顶部搜索对方姓名（支持跨部门搜索）
3. 点击对方名字 → 详情页的「**账号**」就是 userid
4. 记录到本手册 1.3 表格

> 添加文档成员不受「应用可见范围」限制，其他部门的成员也可以直接加。

---

## 六、常见错误码速查

| 错误码 | 含义 | 解决方法 |
|--------|------|---------|
| `40013` | invalid corpid | 企业 ID 填错，确认是「我的企业」里的企业 ID |
| `40014` / `42001` | token 无效/过期 | 重新获取 access_token |
| `48002` | api forbidden | 管理后台「协作→文档→API→可调用接口的应用」没加这个应用 |
| `301085` | invalid docid | 用了客户端建的表。必须用 API（create_doc）建的表 |
| `600011` | 无文档权限 | 应用不是该表创建者。表必须用本应用创建 |

---

## 七、避坑清单（重要）

1. **客户端建的表无法被 API 读取**（报 301085）。凡是需要 API 读写的表，必须用 `create_doc` 命令创建。
2. **docid 只在建表时返回一次**，丢了只能重建表。建完立刻记录。
3. **token 有效期 2 小时**，后端代码里要做缓存和自动刷新（参考 `wecom-smartsheet.mjs`）。
4. **表的所有者是应用**，不能转让给个人。交接负责人 = 用 `mod_doc_member` 加 auth 7，或在企微客户端直接分享。
5. **Secret 和 token 绝不外泄**：不进前端代码、不进 Git、不给维护人员。维护者只需要表格协作权限。
6. **字段名要和代码 key 完全一致**（包括空格，如「模块1/模块3 时间」）。
7. **只写不读的表**（如某些纯收集场景）可以用客户端建表 + Webhook，免建应用流程；需要读就必须走本手册流程。

---

## 八、后续新网站接入检查单

每接入一个新网站，按此清单执行：

- [ ] 确定该网站需要读还是写（读 → 必须 API 建表；只写 → 可用 Webhook）
- [ ] 执行建表命令，保存 docid / url 到本手册
- [ ] get_sheet 确认 sheet_id
- [ ] mod_doc_member 添加自己（auth 7）+ 该网站负责人（auth 7 或 2）
- [ ] 客户端打开表，建好字段（字段名与代码一致）
- [ ] 后端代码新增该表的 docid 配置，实现读/写接口
- [ ] 前端联调测试（写入一条、读取验证）
- [ ] 把表分享给日常维护者，完成交接

---

*手册创建：2026-08-26 · 应用：课程网站--数据接入 · 服务器：60.205.181.202*
